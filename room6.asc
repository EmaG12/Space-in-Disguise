// room script file
bool frecciaUscitaAttiva = false;
function on_mouse_click (MouseButton button)
{
  if (button == eMouseLeft)
  {
  
    if (gInventory.Visible == true)
    {
      return; 
    }
  

    if (Hotspot.GetAtScreenXY(mouse.x, mouse.y) == hUscitaOfficina)
    {
      if (player.ActiveInventory == null)
      {
        ClaimEvent(); 
        Room.ProcessClick(mouse.x, mouse.y, eModeWalkto);
        player.Walk(62, 145, eBlock, eWalkableAreas); 
        player.FaceDirection(eDirectionLeft);
        player.ChangeRoom(4, 828,  256, eDirectionRight); 
      }
    }
  }
}

function Cambio_Cursore()
{

  if (GUI.GetAtScreenXY(mouse.x, mouse.y) != null || IsGamePaused()) 
  {
    frecciaUscitaAttiva = false; 
    return; 
  }

  Hotspot* hSottoMouse = Hotspot.GetAtScreenXY(mouse.x, mouse.y);

  if (hSottoMouse == hUscitaOfficina) 
  {
    if (frecciaUscitaAttiva == false)
    {
      mouse.SaveCursorUntilItLeaves();
      mouse.UseModeGraphic(eModeExitLeft);
      frecciaUscitaAttiva = true;
    }
  }
  else
  {
    frecciaUscitaAttiva = false;
  }
}
function room_RepExec()
{
 Cambio_Cursore();
}

function room_AfterFadeIn()
{
  aSega.Play(eAudioPriorityLow, eRepeat);
  aTrapano.Play(eAudioPriorityLow, eRepeat);
  aJointer.Play(eAudioPriorityLow, eRepeat);

}

function room_Load()
{
  
  aSega.Stop();
  aTrapano.Stop();
  aJointer.Stop();
}

function room_Unload()
{
 aSega.Stop();
 aTrapano.Stop();
 aJointer.Stop();
}
